export enum Protocal {
  Http = 'http://',
  Https = 'https://',
}
export enum Status {
  ALL = 0,
  ON = 1,
  OFF = 2,
}
export enum YesNo {
  ALL = 0,
  YES = 1,
  NO = 2,
}

export enum LevelCode {
  Vendor = 'L1',
  Shareholder = 'L2',
  MajorAgent = 'L3',
  Agent = 'L4',
  Member = 'L5',
}

export enum ProcessStatus {
  Pending = 1,
  Running = 2,
  Finish = 3,
  Cancel = 4,
}

export enum Device {
  PC = 1,
  Mobile = 2,
}
export enum IPStatus {
  Black = 1,
  White = 2,
}

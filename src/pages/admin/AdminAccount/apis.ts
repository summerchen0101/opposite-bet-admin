import Request from '@/utils/request'
import { RequestPromise } from '@/types'

// export const getList: RequestPromise = <T>(data) =>
//   Request.post<T>('admin/getAdminList', data)

// export const create: RequestPromise = <T>() =>
//   Request.post<T>('admin/editAdmin', { method: 'ADD' })

// export const edit: RequestPromise = <T>(id: number) =>
//   Request.post<T>('admin/editAdmin', { method: 'EDIT', admin_id: id })

// export const doCreate: RequestPromise = <T>(data) =>
//   Request.post<T>('admin/storeAdmin', { method: 'ADD', ...data })

// export const doEdit: RequestPromise = <T>(data) =>
//   Request.post<T>('admin/storeAdmin', { method: 'EDIT', ...data })

// export const doDelete: RequestPromise = <T>(id) =>
//   Request.post<T>('admin/storeAdmin', { method: 'DELETE', admin_id: id })

// export const setStatus: RequestPromise = <T>(data) =>
//   Request.post<T>('admin/commonStatusChanger', {
//     module_id: 'Admin',
//     ...data,
//   })
